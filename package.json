{
  "dependencies": {
    "adm-zip": "0.4.14",
    "sort-json": "2.0.0"
  },
  "license": "GPL-3.0",
  "name": "wire-web-config-default",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/wireapp/wire-web-config-default.git"
  },
  "scripts": {
    "release:patch:staging": "npm version prepatch --preid staging",
    "release:major:staging": "npm version premajor --preid staging",
    "release:minor:staging": "npm version preminor --preid staging",
    "release": "npm version patch",
    "postversion": "git push --follow-tags",
    "sync": "git checkout master && git pull && git fetch upstream && git merge upstream/master && git push origin",
    "translate:download": "yarn translate:download:wire-account",
    "translate:download:wire-account": "node ./bin/translation-download.js -p wire-account",
  },
  "version": "0.25.2"
}
